local TextChatService = game:GetService("TextChatService")
local Players = game:GetService("Players")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- GUI
local screenGui = Instance.new("ScreenGui")
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- Main Frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 220, 0, 120)
frame.Position = UDim2.new(0, 20, 0.5, -60)
frame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
frame.Parent = screenGui

-- Close Button
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 25, 0, 25)
closeBtn.Position = UDim2.new(1, -30, 0, 5)
closeBtn.Text = "X"
closeBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
closeBtn.Parent = frame

-- Open Button
local openBtn = Instance.new("ImageButton")
openBtn.Size = UDim2.new(0, 60, 0, 60)
openBtn.Position = UDim2.new(0, 10, 0.5, -30)
openBtn.BackgroundTransparency = 1
openBtn.Visible = false
openBtn.Parent = screenGui

-- Roblox "Man Face" image
openBtn.Image = "rbxassetid://86487700"

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(1, 0) -- perfect circle
corner.Parent = openBtn

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(100, 200, 255)
stroke.Thickness = 2
stroke.Parent = openBtn
openBtn.MouseButton1Down:Connect(function()
	openBtn:TweenSize(UDim2.new(0, 55, 0, 55), "Out", "Quad", 0.1, true)
end)

openBtn.MouseButton1Up:Connect(function()
	openBtn:TweenSize(UDim2.new(0, 60, 0, 60), "Out", "Quad", 0.1, true)
end)

-- Label
local label = Instance.new("TextLabel")
label.Size = UDim2.new(1, -20, 0, 30)
label.Position = UDim2.new(0, 10, 0, 10)
label.Text = "make baldi mad"
label.TextColor3 = Color3.new(1,1,1)
label.BackgroundTransparency = 1
label.Parent = frame

-- Toggle Background (bar)
local toggleBg = Instance.new("Frame")
toggleBg.Size = UDim2.new(0, 100, 0, 40)
toggleBg.Position = UDim2.new(0.5, -50, 0.5, -10)
toggleBg.BackgroundColor3 = Color3.fromRGB(80, 0, 0)
toggleBg.Parent = frame

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(1,0)
corner.Parent = toggleBg

-- Toggle Circle (the button)
local toggleCircle = Instance.new("TextButton")
toggleCircle.Size = UDim2.new(0, 36, 0, 36)
toggleCircle.Position = UDim2.new(0, 2, 0.5, -18)
toggleCircle.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
toggleCircle.Text = ""
toggleCircle.Parent = toggleBg

local circleCorner = Instance.new("UICorner")
circleCorner.CornerRadius = UDim.new(1,0)
circleCorner.Parent = toggleCircle

-- State
local enabled = false

-- Toggle function
toggleCircle.MouseButton1Click:Connect(function()
	enabled = not enabled
	
	if enabled then
		-- ON (green, right)
		toggleBg.BackgroundColor3 = Color3.fromRGB(0, 100, 0)
		toggleCircle.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
		toggleCircle:TweenPosition(UDim2.new(1, -38, 0.5, -18), "Out", "Quad", 0.2, true)
	else
		-- OFF (red, left)
		toggleBg.BackgroundColor3 = Color3.fromRGB(80, 0, 0)
		toggleCircle.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
		toggleCircle:TweenPosition(UDim2.new(0, 2, 0.5, -18), "Out", "Quad", 0.2, true)
	end
end)

-- Close/Open
closeBtn.MouseButton1Click:Connect(function()
	frame.Visible = false
	openBtn.Visible = true
end)

openBtn.MouseButton1Click:Connect(function()
	frame.Visible = true
	openBtn.Visible = false
end)

-- Auto chat loop
task.spawn(function()
	while true do
		task.wait(1)
		
		if enabled then
local args = {
	"YCTP_PermAngry"
}
game:GetService("ReplicatedStorage"):WaitForChild("Notebook"):FireServer(unpack(args))

		end
	end
end)
local UserInputService = game:GetService("UserInputService")

local dragging = false
local dragInput
local dragStart
local startPos

local function update(input)
	local delta = input.Position - dragStart
	frame.Position = UDim2.new(
		startPos.X.Scale,
		startPos.X.Offset + delta.X,
		startPos.Y.Scale,
		startPos.Y.Offset + delta.Y
	)
end

frame.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
	or input.UserInputType == Enum.UserInputType.Touch then
		
		dragging = true
		dragStart = input.Position
		startPos = frame.Position
		
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

frame.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement
	or input.UserInputType == Enum.UserInputType.Touch then
		dragInput = input
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if input == dragInput and dragging then
		update(input)
	end
end)
frame.BackgroundColor3 = Color3.fromRGB(20, 80, 180)
frame.BackgroundTransparency = 0.2 -- 20% transparent

-- Rounded corners
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 20)
corner.Parent = frame

-- Gradient (blue glossy look)
local gradient = Instance.new("UIGradient")
gradient.Color = ColorSequence.new{
	ColorSequenceKeypoint.new(0, Color3.fromRGB(80, 170, 255)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(20, 80, 200))
}
gradient.Rotation = 90
gradient.Parent = frame

-- Glow outline
local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(100, 200, 255)
stroke.Thickness = 2
stroke.Transparency = 0.2
stroke.Parent = frame
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local function addOwnerTag(character)
    local head = character:WaitForChild("Head")

    -- Create BillboardGui
    local billboard = Instance.new("BillboardGui")
    billboard.Name = "OwnerTag"
    billboard.Adornee = head
    billboard.Size = UDim2.new(0, 200, 0, 50)
    billboard.StudsOffset = Vector3.new(0, 2.5, 0)
    billboard.AlwaysOnTop = true

    -- Create text label
    local textLabel = Instance.new("TextLabel")
    textLabel.Size = UDim2.new(1, 0, 1, 0)
    textLabel.BackgroundTransparency = 1
    textLabel.Text = "OWNER"
    textLabel.TextScaled = true
    textLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
    textLabel.Font = Enum.Font.GothamBold
    textLabel.Parent = billboard

    billboard.Parent = head
end

-- Apply when character loads
player.CharacterAdded:Connect(addOwnerTag)

-- If character already exists
if player.Character then
    addOwnerTag(player.Character)
end
